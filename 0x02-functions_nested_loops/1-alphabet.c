#include "main.h"

/**
 * print_alphabet - this function prints alphabets in lower case
 *
 * Return: returning nonthing
 */

void print_alphabet(void)
{
	char u;

	for (u = 'a' ; u <= 'z' ; u++)
		_putchar(u);
	_putchar('\n');
}
