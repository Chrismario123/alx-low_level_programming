#include "main.h"

/**
 * _abs - this function computes the absolute value of a number
 * @m: this is the parameter to be checked
 * Return: absolute value of int
 */
int _abs(int m)
{
	if (m < 0)
	return (-m);
	else if (m >= 0)
	{
		return (m);
	}
return (0);
}
